# jdrdatabase 점검 체크리스트

제3자가 이 저장소를 점검할 때 사용하는 목록이다. 각 항목은 확인 방법이 있어야 하며, "코드 위치"는 구현이 완료된 단계부터 채운다. 항목의 근거는 `docs/DESIGN.md`의 해당 절에 있다.

점검 결과는 항목마다 통과 / 실패 / 해당 없음 중 하나로 적고, 실패는 재현 절차를 함께 남긴다.

## 공통

### 데이터 무결성 (§6.2)

| 항목 | 확인 방법 | 코드 위치 |
|---|---|---|
| 서버에 본문을 쓰는 호출이 `JDR.Notes.save` 밖에 없다 | `updateText`, `createText` 호출처를 전부 찾아 `notes.js` 안에만 있는지 확인 | Step 6 이후 |
| 저장 직전 `modifiedTime` 재확인이 있고, 다르면 쓰지 않는다 | 두 탭에서 같은 노트를 열어 한쪽에서 저장한 뒤 다른 쪽에서 저장 | Step 6 이후 |
| 프론트매터 편집이 본문 바이트를 바꾸지 않는다 | CRLF와 BOM이 있는 픽스처로 셀 편집 후 바이트 비교 테스트 | Step 6 이후 |
| `DEFAULT_SCHEMA`가 어디에도 쓰이지 않는다 | `jsyaml.load(`와 `jsyaml.dump(` 호출에 `schema: CORE_SCHEMA`가 있는지 확인 | Step 4 이후 |
| 로컬 시계로 서버 시간을 비교하는 코드가 없다 | `Date.now()`와 `new Date()`의 사용처가 UI 표시나 디바운스뿐인지 확인 | Step 3 이후 |
| 빈 본문 덮어쓰기와 재직렬화 불일치 시 저장을 거부한다 | 해당 조건을 강제로 만드는 단위 테스트 존재 여부 | Step 6 이후 |
| 오프라인 큐 재생 시에도 충돌 검사를 거친다 | 오프라인 상태에서 저장 후 서버 파일을 바꾸고 온라인 복귀 | Step 3, Step 10 이후 |

### 권한과 보안

| 항목 | 확인 방법 | 코드 위치 |
|---|---|---|
| Drive 요청이 볼트 루트 밖을 대상으로 하지 않는다 | `JDR.Drive`의 모든 목록·조회 요청에 루트 ID 검사가 있는지 확인 | Step 2 이후 |
| 토큰이 `localStorage`나 IndexedDB에 저장되지 않는다 | 저장소 검색과 개발자 도구 확인 | Step 1 이후 |
| 렌더된 HTML이 `DOMPurify`를 거치고 허용 스킴이 `http`, `https`, `mailto`뿐이다 | `javascript:` 링크와 `<script>`가 포함된 노트로 미리보기 | Step 7 이후 |
| 외부 링크가 새 탭과 `rel="noopener noreferrer"`로 열린다 | 렌더 결과의 `<a>` 속성 확인 | Step 5, Step 7 이후 |
| 클라이언트 ID가 저장소에 박혀 있지 않다 | 소스 검색 | Step 0 이후 |
| CDN 스크립트에 고정 버전과 SRI가 있다 | `<script src>` 태그 확인. 빌드 결과에도 유지되는지 확인 | Step 10 이후 |
| 영구 삭제 API 호출이 없다 | `files.delete` 호출처 검색 | Step 6 이후 |

### 오류 처리 (§6.1)

| 항목 | 확인 방법 | 코드 위치 |
|---|---|---|
| 401이 토큰 갱신 후 1회만 재시도된다 | 토큰을 지운 뒤 요청 | Step 1 이후 |
| 429와 5xx에 지수 백오프가 있고 최대 5회다 | `request` 재시도 로직 확인 | Step 2 이후 |
| 오류가 `reportError`로 모이고 삼켜지지 않는다 | `catch` 블록 중 로그만 남기고 끝나는 곳이 없는지 확인 | 전 단계 |
| 파싱 오류 노트가 인덱스에 남고 본문은 표시된다 | 깨진 YAML 픽스처 열기 | Step 4 이후 |

### 성능 (§7)

| 항목 | 확인 방법 | 코드 위치 |
|---|---|---|
| 초기 파싱이 메인 스레드 장기 작업을 만들지 않는다 | 3,000개 가짜 볼트로 `?bench=1` 실행, 50ms 초과 작업 없음 | Step 4 이후 |
| 표 스크롤이 프레임 예산 안에 든다 | 3,000행에서 스크롤 프레임 16ms 이하 | Step 5 이후 |
| 본문 전문이 메모리 인덱스에 올라가지 않는다 | `NoteMeta`에 `body` 필드가 없고 `preview`가 200자로 잘리는지 확인 | Step 4 이후 |
| 정규식이 선형이다 | 20만 자 픽스처가 테스트에 포함되고 시간 제한 안에 끝난다 | Step 4 이후 |

## 단계별

각 단계가 완료될 때 아래 절을 채운다. 채우기 전까지는 `docs/DESIGN.md`의 해당 단계 "예외 처리"와 "완료 기준"이 점검 목록을 대신한다.

### Step 1. 인증

미작성. 채울 항목: 상태 머신 전이, 무음 재발급 실패 시 읽기 전용 전환, 부분 범위 승인 처리.

### Step 2. Drive 클라이언트와 폴더 스캔

미작성. 채울 항목: 바로가기 순환 방지, 동명 파일 배지, 배치 실패 폴백, 공유 드라이브 파라미터.

### Step 3. 캐시와 증분 동기화

미작성. 채울 항목: `pageToken` 만료 폴백, 삭제 감지, 다중 탭 리더 선출, 용량 초과 시 본문 캐시만 삭제.

### Step 4. 파서와 인덱스

미작성. 채울 항목: 픽스처 목록과 각각의 기대 결과, Worker 폴백.

### Step 5. 표 뷰와 뷰 정의

미작성. 채울 항목: `views.json` 손상 복구, 타입 혼합 정렬, 문자열 셀 URL 자동 링크의 이스케이프 순서.

### Step 6. 편집기와 저장

미작성. 채울 항목: 충돌 대화상자 세 선택지, 자동 저장 직렬화, 파일명 정규화, 링크 삽입 이스케이프.

### Step 7. 보드·캘린더·갤러리와 미리보기

미작성. 채울 항목: 드래그 실패 복원, 임베드 깊이 제한, 자동 링크 예외.

### Step 8. 쿼리 언어

미작성. 채울 항목: Dataview 예제 20개와 결과 일치, 파싱 오류 위치 표시.

### Step 9. 스프레드시트 가져오기

미작성. 채울 항목: 멱등성(`appProperties`), 헤더 정규화, 날짜·선행 0 처리, 중단·재개.

### Step 10. 마무리

미작성. 채울 항목: 전문 검색 취소, 오프라인 큐 충돌 목록, 빌드 결과의 SRI 유지, 릴리스 워크플로 실패 시 기존 릴리스 보존.
